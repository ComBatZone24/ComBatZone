// This component is no longer used as the sidebar navigation has been replaced by a bottom navigation bar.
// Keeping the file to avoid breaking direct references if any, but it serves no active purpose.
// Consider removing this file in a future cleanup.

export function SidebarNav() {
  return null;
}
